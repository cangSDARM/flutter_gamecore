import 'package:flutter/material.dart';
import 'package:flutter_app/component/Texts.dart';

class DynamicPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: EmptyText(title: "没有折扣消息"),
    );
  }
}
