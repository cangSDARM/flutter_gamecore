import 'package:flutter/material.dart';
import 'package:flutter_app/component/Texts.dart';

class CommunicationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: EmptyText(title: "没有通知消息"),
    );
  }
}
